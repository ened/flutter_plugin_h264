name: h264_example
description: Demonstrates how to use the h264 plugin.
publish_to: 'none'

dependencies:
  path_provider: any
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  h264:
    path: ../
  e2e: ^0.4.0
  flutter_driver:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/samples/